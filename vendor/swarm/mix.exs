defmodule Swarm.Mixfile do
  use Mix.Project

  def project, do: [
    app: :swarm,
    version: "3.4.0",
    elixir: "~> 1.3",
    deps: []
  ]

  def application, do: [
    applications: []
  ]
end
